{
  "name": "gitbook",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "gitbook serve",
    "prebuild": "gitbook install",
    "build": "gitbook build",
    "predeploy": "gitbook install && gitbook build",
    "deploy": "gh-pages -d _book",
    "prettier": "prettier --write {**/*,*}.{js,html,css,json}"
  },
  "keywords": [],
  "author": "Hugh Ahn",
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "prettier": "2.3.2"
  }
}
